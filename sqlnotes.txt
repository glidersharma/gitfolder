Start sql
cd sql/sql/bin
mysqld --initialize --console
mysqld --console
    Now your local host server is running now open a new cmd and go to bin directory of mysql folder
To change password use command 
:alter user 'root'@'localhost' identified by 'your password"; 
mysqld -u root -p
enter password AnshulSharma
  
 here our database name is my_db

sql is not a case sensetive 
Syntax :CREATE DATABASE database_name
ex: CREATE DATABASE my_db;

Syntax: use Database_name;
ex USE my_db;
 
How to create table 
Syntax :
CREATE TABLE table name(
Column1name data type(size)[constraint],
Column2name data type(size)[constraint],
Column3name data type(size)[constraint],
Column4name data type(size)[constraint]
);

 Syntax :DESC table name;
ex : DESC my_tab;q
 it describe no. of columns in your table
Note :it only shows the info about column only

Show keyword = This cmd is used to show all the table in the database

Syntax: Show Database;
ex: SHOW DATABASES;
It will show all the databases in the system

Syntax: SHOW TABLES ;
it will show all the tables

              DATA TYPE

1. INT or INTEGER 
EX : roll INT(8);

2.DEC OR DECIMAL
EX:price DECIMAL(SIZE,DECIMALPOINT BEFORE COUNT NO.)
4567.23 
SIZE =6
decimalpoint =2

3.char  or character :- it can store upto 255
Syntax: column name CHAR(Size);
ex name Char(30);

4.VARCHAR IT also store upto 255 char
Syntax: name VARCHAR(39);

5.TEXT :it can gold upto 65535 character
Syntax:
column name TEXT;

6.DATE display date yyyy-mm-dd hh:mm:ss
syntax column name DATETIME;

7. TIMESTAMP ;It also disply date and time
Syntax column name TIMESTAMPL;

TO GO TO HELP RUN CMD
HELP CONTENT
THEN CHOOSE WHICH YOU WHANT TO KNOW


            INSERT INTO

Syntax:table name(column1,column2.....)
VALUES(Value1,value2,'value3'......);
its not compulsory to give column 
but we have to make sequence same as in table

Changing the order of column
Syntax:table name(column4,column3.....)
VALUES(Value4,value3,'value5'......);


insert into multiple records at one time
Syntax:table name(column1,column2.....)
VALUES(Value1,value2,'value3'......)
(Value1,value2,'value3'......);


        SELECT 
1.select all column from the table
Syntax: SELECT * FROM TABLENAME;

2.select particular column fron table
syntax: SELECT COLUMN 1,COLUMN 2 ,.....   FROM table name;

solving single cout problem

method 1: use two single couts in place of one single couts 
ex:insert into table name(name)
VALUES('Mr''s Babita');

method 2:
ex:insert into table name(names)
VLAUES('KK\'s company');
  

SELECT with Limit
Syntax : select * from stuid limit NO.;

SELECT with Limit
Syntax : select * from stuid limit START ,NO.OF RECORD


                WHERE 
SYNTAX : select * from table name Where column_name operator 'Value';

Specific data from Specific column

Syntax Select column name
from table name 
where name ='anu';


Syntax Select column name
from table name 
where stu_id=5;

Note value can be text or numeric so use single couts or double couts 
or here operator are =,<,>,?,!=,between ,like ,in;


             NULL VALUE 
IS Null:
Syntax :select * from student where city is null;

IS NOT NULL:

using opreator with select:
Select * from Student where name ='sona' operator studentid=12345;



COMBINATION OF AND & OR


SELECT * from tablename 
where column='value'
operator (column name ='value or column name ='value');


   IN operator
Syntax: SELECT  * FROM TABLE NAME where name in (value ,value2);


   Not in:
Select * from  newtablr where valuue not in ('anu',sonu);

  BETWEEN :
 
Syntax: SELECT  * FROM TABLE NAME where name BETWEEN 105 AND 109;

 BETWEEN :
 
Syntax: SELECT  * FROM TABLE NAME where name BETWEEN 'a' AND 'j';


BETWEEN Date

SELECT  * FROM TABLE NAME whare Dob between '1990-01-01' AnD ''1992-11-01';

    LIKE 
use to search specifired pattern in column
Syntax: 
SELECT * FROM NEW TABLE WHERE COLUMN NAME like'%nu';

WILDCARDS
 wildcards are used to search for data within a table .these character are used with the LIKE operator.

1. %- Zero or more Character 
ex
"Geeky%" -all Starting with geek ex -geeksforgeeks
"%sh%-All containing with sh ex -geekyshows

2._ One single Characters
Show_ Starting with shaow then any character Ex-Shows.


ORDER BY :
SYNTAX :
SELECT * FROM STUDENT ORDER BY column name DESC;

OR USE Asc ;

NOT NULL it is a constraint use at the time of creating table 
it means that that column is compulsory to fill that column otherwise 
it will give you error;

synatx: create table(
name int (5)
site text,
mobile(10) NOT NULL
);

UNIQUE KEY:
 it is also a constraint 
that is like roll no. is a unique key constraint  no two student has same roll no. in a class
unique constraint uniquely identifies each record in a database table


PRIMARY KEY:
primary key constraint uniquely identifies each record in a database table .
primary key must contain UNIQUE value .A primary key column cannot contain NULL values . most table should 
have primary key and each table can have only one primary key.

Synatx 
CREATE TABLE STUDENT (
Name varchar(49)
roll integer (5) NOT NULL PRIMARY KEY,
MOBILE NO. INT (10)
);


AUTO_INCREMENT
auto increment is used to generate an unique number ,when a new record id inserted in a table.

syntax:
create table tablename(
column name int not null AUTO_INCREMENT PRIMARY KEY,
COLUMN NAME VARCHAR(59),
COLUMN NAME TEXT,
);

create table tablename(
column name int not null AUTO_INCREMENT,
COLUMN NAME VARCHAR(59),
COLUMN NAME TEXT,
primary key(name of auto increment col),
);

         ALTER KEYWORD 
It is used to change the starting ir default values in auto Increment
 ex alter table table name auto_increment =10;


  ALIASES 
aliases is used to temporarily rename a table name or a column name.
for table :
Syntax SELSECT COLUMN FORON TABLENAME a NEW NAME;

for column:
Syntax : Select column name as new name from table name;
ex: select name AS student_name from Student ;
SELECT name AS "student name " from Student;
SELECT name AS [student name] from Student;


            AIRTHMATIC OPERATOR
syntax: select column name ,column name operator value from table name ;


            SELECT DISTINCT

SYNTAX : select distinct column name from table id;


            ALTER TABLE            
this command is used to Add/change /modify /Drop existing structure of the table.
1.add column 
2. enable /disable constraint
3.modify column 
4.Drop column

ADD COLUMN

SYNTAX :
alter table table name
add column column name datatype(size);

enter more than one column at a time
Syntax:
alter table table name
add column column name datatype(size),
add column column name datatype(size);

ADD COLUMN by position 
1. LAST 
2. FIRST
3. AFTER

SYNTAX alter table table name 
add column column name datatype (size) first/last/after(column name);

ADD COLUMN name with constraint
syntax add column column name datatype (size) constraints;

when integrity constraint have to be include

Syntax: alter table table name
add constraint unique key(column name);

Change column: this is used to change name and data type of an existing column without constraint.
ex : alter table table name
change column old name new column name new data type(size);

CHANGE MORE THAN ONE COLUMN 
SYNTAX:alter table table name
change column old name new column name new data type(size),
change column old name2 new column name2 new data type2(size);

CHANGE COLUMN NAME WITH CONSTRAINT:-
SYNTAX:ALTER TABLE table name 
change column old name new column name new data type(size) constraint name,
ADD primary key(column name) ;

MODIFY COLUMN THIS IS USED TO MODIFY SIZE OF THE DATA TYPE OR THE DATA TYPE ITSELF OF AN E
EXISTING COLUMN WITHOUT CHANGING COLUMN NAME.

SYNTAX: alter table table name 
modify column column name datatype(size);


DROP COLUMN : WHAEN A COLUMN IN A TABLE NEED TO DELETE.
SYNTAX: 
alter table table name 
DROP COLUMN column name;

when removing constraint from a column.
Syntax:
Alter table table name 
drop constraintname (column name);

DELETE TABLE FROM OUR DATABASE 
SYNTAX : drop table table name;

TRUNCATE TABLE:
WHEN WE ONLY WANT TO DELETE THE DATA INSIDE THE TABLE AND NOT THE TABLE ITSELF.
SYNTAX:- 
TRUNCATE TABLE TABLE NAME;


RENAME TABLE : this cmd is used to rename one or more table.
Syntax: RENAME TABLE OLD NAME TO NEW NAME;

ALTER DATABASE ;
SYNTAX ALTER DATABASE;

DROP DATABASE;
SYNTAX : DROP DATABASENAME ;


SHOW COLUMN :
show column from table name:

SHOW CREATE DATABASE DATABASE:
IT SHOWS CMD WHICH YOU HAVE WRITTEN WHILE CREATING YOUR DATABASE;
SYNTAX: show create database database name;

show creating table 
Syntax :
Show create tablename;


UPDATE : THE update statement is used to update existting record in a table.
update table name
SET  column1=value1,column2 =value2,............
where somecolumn =some value;

ex:
UPDATE EMP
SET EMP NAME ='ARJUN',salary=35000
where empid =101;

NOTE: WHERE IS NECESSARY OTHERWISE ALL RECORDS WILL BE REPLACE WITH GIVEN VALUE

Update with case 
Syntax :
update tablename
set Result=
CASE
     when column name 1=some value 1then new value1
     when column name 2=some value 2then new value2
     ELSE  NEW VALUE 3
END;

we can also use airthmetric operator in place of =;


DELETE 
the delete statement is used to delete record in atable.
Syntax :
Delete from table name 
where some column =some value

NOTE WHERE IS NECESSARY OTHERWISE ALL RECORD WILL BE DELETE

DELETE ALL RECORDS

syntax : delete from emp:
or delete * from table;


COPY OLD TABLE TO NEW TABLE IN SAME DATABASE
SYNTAX:
create table new table like oldtable;
insert teacher select * from student;


COPY OLD TABLE TO NEW TABLE
WITHIN different database
use the database where you want to copy the old table
Syntax :-
create table new table like olddatabase.oldtable;
insert teacher select * from student;

     
   FUNCTION IN SQL
 1. MIN
select MIN(COLUMN) From table;

2.MAX
select MAX(column) as maximum_salary from table;

3.SUM 
select SUM(column) as TOTAL_salary from table;

4.AVERAGE
select average(column) as average_salary from table;

5.SQUARE ROOT
select salary,squrt(salary) from table;

DECIMAL DATA TYPE
syntax :- columnname decimal(t,d);
where 
t=total digit 1-65;
d=digit after decimal.range 0-30 and must not be more than t.

ex 2345677654.32
d=2,t=12;

by default value of t=10 and d=0.

  zerofill is a key word
syntax column name decimal(10,4) zerofill;


ROUND
SYNTAX:- select * ,round(price,1) from table name;

COUNT(): - THIS function return the no. of values (null values will not be counted) of the specified column.

Syntax:- select count(column name) from table name;
count(*) will give count of all record;


using distinct
select count(distinct column name) from table name;

UPPER(COLUMN NAME):-
syntax:select upper(column name) from table ;


LOWER(COLUMN NAME):-
syntax:select LOWER(column name) from table ;
 

MID OR SUBSTRING(COLUMN NAME ,START,LENGTH)
SYNTAX: select MID(city,1,3) from table;

LENGTH(COLUMN )
syntax: select column name ,length(column)from table;

CONCAT(COL1,COL2,)
SYNTAX: select column name ,concat(city,name) from table;

REVERSE(COLUMN NAME)
syntax:  select reverse(city) from table;

NOW() IT RETURN THE CURRENT time and date;
syntax select column ,column ,now() from table;

GROUP BY
SYNTAX:select name, min(salary) from table group by name;

having:
SYNTAX:select name, min(salary) from table group by name having min(salary)>25000;

                    RELATIOANSHIP
1.one to one
2.one to many
3.many to many

NORMALIZATION
it is used to remove duplicaey in table
1NF
2NF
3NF

FORIGN KEY







 